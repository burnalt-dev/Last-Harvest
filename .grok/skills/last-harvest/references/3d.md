# 3D shift (Ivan 2026-09-03)

**Plan:** Solace is **3D**. Terrain is **2D iso**. Same Vite stack. No Unity.

**Now (Ivan 2026-09-05):** Title **3D Bay** + dungeon **Drop3D** (`src/game/solace3d/`). Default = **shells + scythe** (armor `none` = gunmetal shell; both hands `p018`) + cute **Gasket** mesh + `buildFoe`. Terrain stays **2D iso textures** on occupancy diamonds. Dual renderer is **dated**: Bay and drop are both real 3D actor scenes; floor is not a mesh. Maker lock: `last-harvest-makers`.

**Enemies:** 3D `foe3d.ts` on the drop. Packed PNG sheets are look refs only.

**Gasket:** one cute 3D default (`gasket.ts`, not modular). Socket **`gasket_mount`**. Same mesh in Bay and drop.

**Modular:** 3D **sockets** (spine / arms / legs / mounts). Parts parent on. One mesh, yaw — **not** Imagine 4-dir PNGs. B1 packer is **parked**.

**Scale:** Front Mission, **one iso tile**. Chubby OK, must be able to harvest-swing a giant two-hand scythe. Crane arms must read (shoulder → upper → elbow → forearm → fist).

**Look:** north-star sheet still the silhouette. Pixel canvas mats + toon ramp + dark outline. No real-time shadows (they clash with 2D tiles).

## Maker skills (3D path)

| # | Skill | Job | Status |
|--|--|--|--|
| **A** | `last-harvest-3d` | Bay + Drop3D scene, lazy `three` | live |
| **B** | `solace-rig` | Bare sockets, harvest swing | live |
| **C** | `solace-toon` | Pixel mats in `tex.ts` until messy | dummy |
| **D** | `last-harvest-iso3d` | Mesh / voxel Veldt floor | **dropped** |
| **E** | `last-harvest-foe3d` | Simple 3D foes | live |
| **G** | `solace-gold` | Labor 1 sit numbers + leg sliders | live |
| **P** | `solace-parts3d` | Crude plates parent onto sockets. Default `none` | live |

**Clips:** `solace-mech-anim` (3D). Harvest is live in `kit.swing()`.

## Fail

- Imagine 4-dir factory as the way forward
- Dual renderer forever without a date (dated **2026-09-05**: Bay + Drop3D are both real; floor stays 2D tex on diamonds)
- 3D **mesh** floor / square grid in the dungeon
- 3D shadows + 2D sprites
- Modular Gasket
- Giant crate spanning 2+ tiles
- Arms that vanish into the chest
